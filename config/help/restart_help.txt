Command: restart

Usage: 
  craft restart -n <server_name>     Usage details: Run '$ craft restart -h'

Required:
  -n <server_name>                   Name of server to restart

Options: 
  -m                                 Monitor the server process for failure.
                                     If the serveris down it will be restated 
                                     automatically.